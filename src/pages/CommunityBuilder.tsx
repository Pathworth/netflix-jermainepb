import React from "react";

export default function CommunityBuilder() {
  return (
    <main style={{ minHeight: "100vh", background: "#000", color: "#fff" }}>
      <section
        style={{
          maxWidth: "960px",
          margin: "0 auto",
          padding: "80px 20px",
          textAlign: "left",
        }}
      >
        <h1 style={{ fontSize: "3rem", marginBottom: "20px" }}>
          Community Builder
        </h1>
        <p style={{ fontSize: "1.1rem", lineHeight: 1.6 }}>
          This is a placeholder page for the Community Builder pillar. We will
          swap this with the full design later. For now it only exists so the
          site can build and your Netflix home screen can load correctly.
        </p>
      </section>
    </main>
  );
}
