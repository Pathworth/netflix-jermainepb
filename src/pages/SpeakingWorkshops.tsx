import React from "react";

export default function SpeakingWorkshops() {
  return (
    <main style={{ minHeight: "100vh", background: "#000", color: "#fff" }}>
      <section
        style={{
          maxWidth: "960px",
          margin: "0 auto",
          padding: "80px 20px",
          textAlign: "left",
        }}
      >
        <h1 style={{ fontSize: "3rem", marginBottom: "20px" }}>
          Speaking &amp; Workshops
        </h1>
        <p style={{ fontSize: "1.1rem", lineHeight: 1.6 }}>
          This is a placeholder page for the Speaking &amp; Workshops pillar.
          Once the shell is working, we will replace this with your full
          speaking page layout.
        </p>
      </section>
    </main>
  );
}
